{{-- @props(['options', 'name', 'selected' => ''])
<select name="{{ $name }}" id="{{ $name }}">
    @foreach ($options as $key => $value)
        <option value="{{ $key }}" @if ($key === $selected) selected @endif>{{ $value }}
        </option>
    @endforeach
</select> --}}
